apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pvc-q12
spec:
  accessModes:
    - ReadWriteOnce
  volumeMode: Filesystem
  resources:
    requests:
      storage: 2Gi
  storageClassName: state