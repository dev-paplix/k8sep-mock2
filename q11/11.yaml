apiVersion: v1
kind: Pod
metadata:
  name: nginx
spec:
  containers:
  - name: nginx
    image: nginx:1.14.2
    ports:
    - containerPort: 80
    volumeMounts:
    - mountPath: /data
      name: vol-local
  volumes:
  - name: vol-local
    persistentVolumeClaim:
      claimName: pvc